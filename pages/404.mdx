import FaqCategories from "./faqs/_meta.json";
import Heading from "../components/Heading";
import React from "react";
import Sitemap from "../components/Sitemap";

<div className="text-left px-4 lg:px-[79px] pb-10">
    <header className="py-20 flex flex-col gap-6">
        <Heading level={1} className="text-center lg:text-left">Oops, page not found!</Heading>
        <p  className="text-center lg:text-left">Feel free to try some of the links below!</p>
    </header>

    <Sitemap/>

</div>
