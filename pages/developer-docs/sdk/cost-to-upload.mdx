---
description: Gets the cost to upload N bytes of data to Bundlr
---

## Getting Upload Cose

## `bundlr.getPrice()`

Returns the cost to upload the specified number of bytes.

The value returned is in context of the token specified when connecting to the node.

The value returned is in atomic units, to convert to standard format use the function [`bundlr.utils.fromAtomic(atomicUnits)`.](/developer-docs/sdk/utility-functions)

The following code example determines the cost to upload 1 megabyte of data.

```js
// Check the price to upload 1MB of data
// The function accepts a number of bytes, so to check the price of
// 1MB, check the price of 1,048,576 bytes.
const dataSizeToCheck = 1048576;
const price1MBAtomic = await bundlr.getPrice(dataSizeToCheck);

// Convert from atomic units to standard units
const price1MBConverted = bundlr.utils.fromAtomic(price1MBAtomic);
console.log(`Uploading 1MB to Bundlr costs ${price1MBConverted}`);
```
